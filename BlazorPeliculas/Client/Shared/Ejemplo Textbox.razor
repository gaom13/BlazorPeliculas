@* Los atributos (@attributes) de leen de derecha a izquierda *@

<input type="text" placeholder="Placeholder por defecto" @attributes="ParametrosAdicionales"/>

@code {
    [Parameter(CaptureUnmatchedValues =true)]
    public IDictionary<string, object> ParametrosAdicionales { get; set; } = null!;
}
